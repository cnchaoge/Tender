# 学习计划：Claude Code

> 根据你的情况定制：零基础 | 工作应用 | 每天 2 小时，边用边学

## 一、概述

系统掌握 Claude Code 的使用，达到在 Tender 项目日常开发中能熟练用 Claude Code 完成编码、调试、重构、文档等任务的水准。

- **当前水平**：零基础，首次接触 AI 编程助手
- **学习目标**：日常开发中高效使用 Claude Code
- **建议总时长**：共 6 周（约 60 小时）
- **核心理念**：本计划全程"边用边学"，从第一天开始就在**这个项目中实战操作**

---

## 二、前置要求

- 已安装 Claude Code CLI（终端输入 `claude` 可用）
- 已登录 Anthropic 账号
- 本项目的 Tender 仓库已在本地
- 基本的命令行操作（`cd`、`ls`、`cat`、`git` 等基础命令）

---

## 三、学习路线

### 第1阶段：第一天上手 —— 跑通基本流程

**预估时间**：3 天（约 6 小时）

**目标**：能在 Claude Code 中完成最基础的对话式编程操作

**学习内容**：

1. **启动与基本对话**
   - 在 Tender 项目目录下输入 `claude` 启动
   - 尝试各种提问方式：
     - 问代码：`解释一下 server/main.py 的作用`
     - 问架构：`这个项目的目录结构是什么样的？`
     - 问配置：`.env 文件里有哪些配置项？`
   - 学会用 `/help` 查看内置命令

2. **理解 Claude Code 的工作模式**
   - Claude Code 是**对话式编程助手**——你说需求，它帮你写代码
   - 它可以直接**读取、修改、创建文件**
   - 它可以**运行 shell 命令**（git、pip、npm 等）
   - 它不是魔法——**问题描述越清晰，结果越好**

3. **第一个实战任务**
   - 在 Tender 仓库里找一个你之前看不懂的文件，问 Claude Code 解释
   - 比如：`server/api/rag.py 这个文件是做什么的？逐行解释`

4. **两个重要概念的理解**
   - **权限提示**：Claude Code 在修改文件或运行命令时会请求权限
   - **工具调用**：后台可以看到 Claude Code 调用了什么工具（Read、Edit、Bash、Write）

**实践建议**：
- 今天就开始用，不用等学完再上手
- 遇到问题直接问 Claude Code 本身："我该怎么用你？"
- 重点关注 **权限机制** 和 **对话方式** 这两个核心概念

**里程碑**：能在 Tender 项目中用 Claude Code 看懂代码、问问题、理解回答

---

### 第2阶段：核心操作 —— 让 Claude Code 帮你改代码

**预估时间**：1.5 周（约 15 小时）

**目标**：能用 Claude Code 完成日常编码任务——改 bug、加功能、重构

**学习内容**：

1. **编辑代码**
   - 明确告诉 Claude Code 要改什么
   - 描述问题 -> Claude Code 理解 -> 给出修改方案 -> 申请权限 -> 执行修改
   - `帮我把 RAGChat.vue 的输入框改成按回车发送`
   - `在 bid.py 里加一个日志，记录每次生成请求的时间`

2. **Git 操作**
   - 让 Claude Code 帮你查看 git 状态、查看 diff、提交代码
   - `git status 看一下`
   - `帮我把这次修改提交，信息写清楚`
   - `对比一下上次提交改了哪些内容`

3. **调试与排查**
   - 遇到报错，**直接把错误信息贴给 Claude Code**（最佳实践）
   - `运行 uvicorn 报错了，这个日志是什么意思？`
   - `我改了 KnowledgeBase.vue 后页面不显示了，帮我看问题在哪`
   - Claude Code 会自动读取相关文件来分析问题

4. **理解 Tender 项目的全貌**
   - 让 Claude Code 带你过一遍项目结构
   - `这个项目前后端是怎么通信的？`
   - `API 的路由是怎么注册的？`
   - `这个项目的数据库架构是什么样的？`

**常见误区**：
| 不要做 | 应该做 |
|--------|--------|
| 问完就等着，不去理解修改了什么 | **审查 Claude Code 的每次改动的 diff** |
| 只问一个问题就不管了 | **持续追问**，让 Claude Code 逐步深入 |
| 怕问"低级问题" | 直接问 —— 这是最快的**学习方式本身** |
| 让 Claude Code 改完不看直接提交 | **必须审查代码**，确保理解和正确 |

**里程碑**：能在日常开发中用 Claude Code 改代码、查 bug、提交 git，每次改完都审查 diff

---

### 第3阶段：进阶能力 —— 熟悉项目级操作

**预估时间**：1.5 周（约 15 小时）

**目标**：能驾驭更复杂的任务——跨文件修改、项目级重构、理解架构

**学习内容**：

1. **跨文件修改**
   - 改一个功能涉及前端+后端的场景
   - `在知识库里加一个"文档标签"功能，前端加标签选择器，后端加字段和 API`
   - Claude Code 会自动找到需要改哪些文件，批量修改

2. **重构项目**
   - 安全的重构方式：让 Claude Code 分析现有代码，规划重构方案
   - `bid.py 太大了，帮我分析一下怎么拆分更合理`
   - `帮我把 api/index.js 里的重复请求逻辑提取成公共方法`

3. **使用 Context / 记忆功能**
   - 用 **CLAUDE.md** 让 Claude Code 了解项目规范
   - 用 **Memory** 让 Claude Code 记住你的偏好
   - 理解这三种持久化机制的区别：
     - CLAUDE.md：项目级指令，团队共享
     - Memory：个人偏好，跨对话持久
     - 对话历史：当前对话上下文

4. **理解技能（Skills）系统**
   - 系统中已安装的技能（如 `/系统学习`）
   - 技能是什么：预定义的指令模板，让 Claude Code 知道怎么处理特定任务
   - 你刚创建的这个 `/系统学习` 技能，就是 Claude Code 来执行这套流程

5. **Slash 命令**
   - `/help` —— 查看可用命令
   - `/clear` —— 清空对话
   - `/cost` —— 查看当前对话消耗
   - `/context` —— 查看上下文使用情况

**实践建议**：
- 在 Tender 仓库里找一个你想改进的小功能，让 Claude Code 实现完整从方案到代码
- 练习用一条清晰的 prompt 描述一个完整的任务

**里程碑**：能独立用 Claude Code 完成一次涉及前后端的完整功能开发，理解项目的整体架构

---

### 第4阶段：高效工作流 —— 提升效率

**预估时间**：1 周（约 10 小时）

**目标**：建立高效的 Claude Code 工作习惯，掌握高级用法

**学习内容**：

1. **Prompt 技巧**
   - **具体比模糊好**：`给 bid.py 的 generate 函数加个超时重试` vs `帮我优化一下`
   - **一次只做一件事**：一句话只说一个诉求
   - **给足上下文**：`server/api/bid.py 第 200 行的 parse_requirements 函数里...`
   - **分步进行**：先分析再改，先改一个文件再改另一个

2. **审查代码的习惯**
   - 这是最重要的习惯：**永远审查 Claude Code 的每次修改**
   - Claude Code 不是人，它不会像你一样理解业务逻辑
   - 每次改动生成后，看 diff，确认改对了再提交
   - 遇到看不懂的修改，让 Claude Code 解释

3. **使用思考模式**
   - 复杂任务启用思考模式（如果可用）：让 Claude Code 先想清楚再动手
   - 适合：架构设计、跨文件重构、复杂逻辑

4. **多轮对话的策略**
   - 第一轮：说清楚你要什么
   - 第二轮：Claude Code 给出方案，你确认
   - 第三轮：执行修改
   - 第四轮：审查结果，反馈修正
   - 第五轮：提交

**实践建议**：
- 在 Tender 里找一个中等难度的功能，完整走一遍上述的多轮对话流程
- 专门练习"给一个模糊需求 -> 逐步引导 -> 精确实现"的流程

**里程碑**：形成了自己的 Claude Code 工作流程，能高效地描述任务、审查结果、迭代改进

---

### 第5阶段：玩转 Tender —— 实际项目管理

**预估时间**：1 周（约 10 小时）

**目标**：能用 Claude Code 完整负责 Tender 项目的日常开发维护

**学习内容**：

1. **与 CLAUDE.md 配合**
   - CLAUDE.md 是项目的"操作手册"
   - 让 Claude Code 知道项目约定、命令、架构
   - 可以随时让 Claude Code 更新 CLAUDE.md

2. **开发全流程**
   - 分析需求 → 设计方案 → 实现代码 → 测试验证 → 提交 PR
   - 每一步都可以让 Claude Code 参与

3. **了解局限性**
   - Claude Code 擅长：代码生成、重构、解释、调试
   - Claude Code 不擅长：需要大量业务上下文判断的场景、UI 视觉精确还原
   - 什么时候应该自己做，什么时候交给 Claude Code

4. **创建自己的技能**
   - 把常用工作流做成 skill，一键调用
   - 例如：创建一个 `tender-bid-workflow` 技能，封装投标功能的常见操作

**实践建议**：
- 在 Tender 里选一个你之前想做的改进，用 Claude Code 完整落地
- 尝试创建一个你自己的小 skill

**里程碑**：能在 Tender 项目中独立用 Claude Code 完成从需求到 PR 的全流程，遇到问题知道如何高效求助

---

## 四、推荐资源

- 📖 **官方文档**：Claude Code 内置 `/help` 命令，随时查看
- 🎬 **内置系统提示**：输入 `claude --help` 查看 CLI 选项
- 🛠️ **实践平台**：**就是你现在打开的这个 Tender 仓库**
- 📚 **CLAUDE.md**：这个项目的 CLAUDE.md 就是你最好的参考文档
- ⚙️ **配置文件**：`~/.claude/settings.json` —— 插件、权限、主题等都在这配
- 🔌 **Plugin 市场**：`/plugin marketplace add` —— 扩展 Claude Code 功能

## 五、实战项目建议

### 初级项目（第 1-2 周）
- **改一个单文件 bug**：在 Tender 里找一个小的界面问题，让 Claude Code 修复
- **加一个字段**：在 Settings 页面加一个新的配置项

### 中级项目（第 3-4 周）
- **跨文件功能改进**：给 RAGChat 加一个"对话历史导出"功能
- **重构**：把 bid.py 里的重复逻辑提取成公共函数

### 进阶项目（第 5-6 周）
- **从零加一个页面**：在 Tender 里加一个"使用统计"页面（后端 API + 前端页面）
- **创建自定义 skill**：把你最常用的工作流封装成一个 skill

## 六、下一步方向

完成本计划后，可以继续探索的方向：

- **Claude API**：不只是 CLI，可以直接调用 API 做自己的 AI 应用
- **MCP 服务**：扩展 Claude Code 的能力，连接数据库、API、文件系统
- **自定义 Hooks**：设置自动化钩子（提交前检查、自动测试等）
- ** Agent 模式**：在复杂任务中使用多 agent 协作
- **开发 Claude Code 插件**：发布到插件市场，给别人用
