# 六、技术架构

## 系统架构图

```
┌─────────────────────────────────────┐
│          ClawOS X 客户端             │
│  浏览器 (Vue3 SPA)                   │
└─────────────────────────────────────┘
                │ HTTP
                ↓
┌─────────────────────────────────────┐
│          FastAPI 后端服务            │
│  Python + SQLite + ChromaDB          │
│  ├── 认证 (JWT)                      │
│  ├── RAG 检索引擎                   │
│  ├── 标书解析 / 生成                │
│  ├── 废标检测                       │
│  └── 管理后台                       │
└─────────────────────────────────────┘
                │ HTTPS
                ↓
┌─────────────────────────────────────┐
│       AI 模型 API (DeepSeek 等)      │
└─────────────────────────────────────┘
```

## 数据存储

| 存储 | 内容 |
|------|------|
| **SQLite** | 元数据（用户、文档记录、配置等） |
| **ChromaDB** | 向量数据（文档切片） |
| **文件系统** | `data/uploads/` 原始文件，`data/generated/` 生成文件 |

## 关键技术栈

| 层级 | 技术 |
|------|------|
| 前端 | Vue3 + Vite + Element Plus |
| 后端 | Python FastAPI |
| 数据库 | SQLite（结构化数据） + ChromaDB（向量） |
| AI | DeepSeek API（LLM）+ 通义 Embedding |
| 部署 | PyInstaller 单文件打包 |

---

## 版本信息

- 当前版本：v1.0 MVP
- 最后更新：2026-05-15
- 产品主页：https://github.com/cnchaoge/clawosx